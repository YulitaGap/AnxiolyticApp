import UIKit

/**
  Stores all the models related to the `ToolsViewController`.
  The `ToolsViewModel's` class will be related to each component.
  The view model will contain Request, Response and ViewModel structs.
 */
struct ToolsViewModel {
    var backgroundColour: UIColor = Attributes.Colors.truewhite
}
