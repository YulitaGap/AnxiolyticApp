import UIKit

/**
  Stores all the models related to the `SettingsViewController`.
  The `SettingsViewModel's` class will be related to each component.
  The view model will contain Request, Response and ViewModel structs.
 */
struct SettingsViewModel {
    var backgroundColour: UIColor = Attributes.Colors.truewhite
}
