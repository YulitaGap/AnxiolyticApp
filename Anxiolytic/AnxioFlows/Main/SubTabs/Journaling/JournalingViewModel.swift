import UIKit

/**
  Stores all the models related to the `JournalingViewController`.
  The `JournalingViewModel's` class will be related to each component.
  The view model will contain Request, Response and ViewModel structs.
 */
struct JournalingViewModel {

}
